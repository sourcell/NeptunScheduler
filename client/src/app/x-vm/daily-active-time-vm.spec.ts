import { DailyActiveTimeVm } from './daily-active-time-vm';

describe('DailyActiveTimeVm', () => {
  it('should create an instance', () => {
    expect(new DailyActiveTimeVm()).toBeTruthy();
  });
});
