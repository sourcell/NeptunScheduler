import { DailyActiveTimeDto } from './daily-active-time-dto';

describe('DailyActiveTimeDto', () => {
  it('should create an instance', () => {
    expect(new DailyActiveTimeDto()).toBeTruthy();
  });
});
